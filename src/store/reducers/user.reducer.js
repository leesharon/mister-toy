const INITIAL_STATE = {
    loggedInUser: {
        name: 'Puki',
    }
}

export function userReducer(state = INITIAL_STATE, action) {

    switch (action.type) {

        default:
            return state
    }
}